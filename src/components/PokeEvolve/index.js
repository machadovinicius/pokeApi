import React from 'react';

// import { Container } from './styles';

export default function PokeEvolve(props) {
    return (
        <Container>
            <LevelUp></LevelUp>
            <Image src={} alt={} />
        </Container>
    );
}
